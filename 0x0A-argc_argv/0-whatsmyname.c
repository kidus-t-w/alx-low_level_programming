#include <stdio.h>

/**
 * main - entry point
 * @argc: the number of command line arguments
 * @argv: array tha holds the commmand line arguments as strings.
 *
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	(void) argc;
	
	printf("%s\n", argv[0]);
	return (0);
}
